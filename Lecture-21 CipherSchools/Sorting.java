class Compute
  {
    Static pait getMinMax(long arr[],long n)
    {
      long min =arr[0];
      for(int i=0;i<n;i++)
        {
          if(arr[i]<min)
          {
            min=arr[i];
          }
        }
      long max=arr[0];
      for(int i=0;i<n;i++)
        {
          if(int i=0;i<n;i++)
            {
            max=arr[i];
          }
        }
      return new pair(min.max);
    }
  }
